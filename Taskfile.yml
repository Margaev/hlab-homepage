# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: "3"

tasks:
  docker-push:
    vars:
      IMAGE_NAME: '{{.IMAGE_NAME | default "homelab:5001/homepage:latest"}}'
    cmds:
      - docker buildx build --platform linux/amd64,linux/arm64 -t {{.IMAGE_NAME}} --push .
